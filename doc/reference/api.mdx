---
title: API reference
description: Dart API surface for restart_app.
---

## Restart.restartApp

Restarts the Flutter application and returns a structured result.

```dart
static Future<RestartResult> restartApp({
  RestartMode mode = RestartMode.platformDefault,
  String? webOrigin,
  String? notificationTitle,
  String? notificationBody,
  bool forceKill = false,
})
```

By default, this uses the best supported behavior for the current platform. Pass `mode` when you want a specific restart path, such as `RestartMode.process`, `RestartMode.flutterEngine`, or `RestartMode.notificationFallback`.

### Web destinations

`webOrigin` controls the web destination:

- Null or empty reloads the current URL and preserves its route.
- A hash-only value such as `#/home` updates the fragment and reloads. Changing the fragment adds a history entry.
- A full or relative URL targeting the same document replaces the current history entry and reloads, including when only the fragment changes.
- A different document uses `window.location.replace`, so navigation replaces the current history entry.

Relative URLs resolve against `document.baseURI`, including any HTML `base` element. Persist required state before calling; code after the request may not finish before the document unloads.

## Restart.restartCapability

Returns the restart behavior available on the current platform.

```dart
static Future<RestartCapability> restartCapability()
```

Use this when your app should adapt UI or behavior to the current platform.
Capability flags describe the implementation, not whether the next restart will
succeed. On web, default reload is supported even though all three restart flags
are false. Missing registration, platform errors, and malformed capability
responses return unavailable capabilities rather than throwing a channel error.

## RestartMode

| Value | Description |
| --- | --- |
| `platformDefault` | Uses the best behavior available for the current platform. |
| `flutterEngine` | Recreates the Flutter engine in the same native process where supported. |
| `process` | Requests a native process relaunch where the platform supports it. |
| `notificationFallback` | iOS-only legacy fallback that schedules a local notification, exits, and requires the user to tap the notification to reopen the app. |

## RestartResult

| Field | Description |
| --- | --- |
| `success` | Whether the platform accepted and initiated the requested restart. |
| `mode` | The mode that was accepted by the platform. |
| `code` | Platform-specific error code when `success` is false. |
| `message` | Platform-specific error message when `success` is false. |

## RestartCapability

| Field | Description |
| --- | --- |
| `fullProcessRestart` | Whether this platform can relaunch the app in a fresh native process. |
| `flutterEngineRestart` | Whether this platform can recreate Flutter in the current native process. |
| `notificationFallback` | Whether notification fallback is available on the current platform. |
| `engineRestartConfigured` | Whether Flutter engine restart has been configured by the host app. |
| `platformDefaultMode` | The behavior used by `RestartMode.platformDefault`. |
| `reason` | Optional explanation when a capability is unavailable or limited. |

## Results and failures

`success: true` acknowledges acceptance, not a completed relaunch. The old Dart
isolate may stop before subsequent work finishes. Persist and await required
state before calling restart, and verify a fresh boot when testing your app.

Platform errors become failed `RestartResult` values. Missing plugin registration
returns `MISSING_PLUGIN`; an unexpected response returns `UNKNOWN_RESULT`.
Initialize the Flutter binding before invoking platform channels before `runApp`.
Prevent duplicate requests while a restart is pending.
Native Android and desktop implementations reject overlapping requests with
`RESTART_ALREADY_IN_PROGRESS`. iOS uses `IOS_RESTART_ALREADY_IN_PROGRESS` for
both engine restart and notification fallback. Retry only after a failure has
been handled or a later intentional user action.

On failure, `mode` preserves the requested mode. On success, it reports the
resolved mode returned by the native implementation. Legacy native responses
that only return `ok` retain the requested mode.
